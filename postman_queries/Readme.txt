If having postman, this can be imported since its JSON files exported with collection v2.1.
Remember that some types of specific get functions require a specficic id in the params.
Get all and use that id for a specific household (as example).